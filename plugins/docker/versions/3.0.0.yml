version: 3.0.0
releaseDate: 2025-12-01T00:00:00Z
minGlideVersion: 3.0.0

# GitHub release URL (update when release is published)
releaseURL: https://github.com/glide-cli/glide-plugin-docker/releases/tag/v3.0.0

# Checksums for each platform (update with actual values after release)
checksums:
  darwin-amd64: sha256:pending
  darwin-arm64: sha256:pending
  linux-amd64: sha256:pending
  linux-arm64: sha256:pending
  windows-amd64: sha256:pending

changelog: |
  ### Breaking Changes
  - Requires Glide v3.0.0+ with SDK v2
  - Migration to SDK v2 type-safe configuration system

  ### Changed
  - Replace v1.BasePlugin with v2.BasePlugin[Config]
  - Add type-safe Config struct with defaultProfile, autoDetectFiles, and projectName options
  - Use declarative SDK v2 pattern with unified lifecycle management
  - Add Docker health check in HealthCheck()
  - Declare RequiresDocker capability in metadata

commands:
  - name: up
    description: Start Docker services
  - name: down
    description: Stop Docker services
  - name: restart
    description: Restart Docker services
  - name: logs
    description: View service logs
  - name: ps
    description: List running containers
  - name: exec
    description: Execute command in container
  - name: build
    description: Build Docker images
  - name: pull
    description: Pull Docker images
  - name: stop
    description: Stop containers
  - name: start
    description: Start stopped containers
  - name: rm
    description: Remove stopped containers
  - name: prune
    description: Remove unused resources
  - name: stats
    description: Display container resource usage
  - name: top
    description: Display running processes in containers
  - name: health
    description: Check health of services
